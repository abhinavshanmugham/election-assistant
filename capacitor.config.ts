import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.abhinav.electionassistant',
  appName: 'election-assistant',
  webDir: 'build'
};

export default config;
